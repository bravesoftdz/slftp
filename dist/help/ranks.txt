Displays the current site ranks.
Usage: !ranks [sectionname]
If sectionname is omitted, slftp will display all the sites 
where the rank is locked (see !ranklock), else slftp will display 
all the ranks belonging to the specified section.
